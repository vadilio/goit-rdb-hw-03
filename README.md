# SQL Домашня робота: базові SELECT-запити (MySQL)

## 📌 Опис

Цей репозиторій містить рішення домашньої роботи з SQL (MySQL). Завдання
спрямовані на відпрацювання базових операцій вибірки даних, агрегатних
функцій, сортування, групування та фільтрації даних.

Усі SQL-запити перевірені на коректність виконання в **MySQL
Workbench**.

------------------------------------------------------------------------

## 🛠 Використані інструменти

-   **MySQL Server**
-   **MySQL Workbench**
-   SQL (синтаксис MySQL)

------------------------------------------------------------------------

## 📂 Структура репозиторію

    .
    ├── sql/
    │   ├── sql1_1.sql   # SQL-запити для виконання завдання 1.1
    │   ├── sql1_2.sql   # SQL-запити для виконання завдання 1.2
    │   ├── sql2.sql     # SQL-запити для виконання завдання 2
    │   ├── sql3.sql     # SQL-запити для виконання завдання 3
    │   ├── sql4.sql     # SQL-запити для виконання завдання 4
    │   └── sql5.sql     # SQL-запити для виконання завдання 5
    ├── README.md        # Опис домашньої роботи
    ├── Dump_hw3.sql     # Дамп бази данних
    ├── p1_1_Вибір стовпчиків з таблиць products і shippers, Усі стовпчики з products.png               # Скріншот до завдання 1.1 
    ├── p1_2_Вибір стовпчиків з таблиць products і shippers, Лише name, phone з shippers.png            # Скріншот до завдання 1.2
    ├── p2_Середнє, максимальне та мінімальне значення price з products.png                             # Скріншот до завдання 2
    ├── p3_Унікальні пари category_id та price, відсортувати за price спаданням, показати 10 рядків.png # Скріншот до завдання 3
    ├── p4_Кількість продуктів у межах ціни від 20 до 100 (включно).png                                 # Скріншот до завдання 4
    └── p5_Кількість продуктів і середня ціна для кожного supplier_id.png                               # Скріншот до завдання 5


------------------------------------------------------------------------

## ✅ Завдання та рішення

### 1. Вибір даних із таблиць

-   Вибрати всі стовпчики з таблиці `products` за допомогою wildcard `*`
-   Вибрати лише стовпчики `name` та `phone` з таблиці `shippers`

Використовуються базові SQL-оператори `SELECT`.

------------------------------------------------------------------------

### 2. Агрегатні функції

Знайти: - середнє значення (`AVG`) - максимальне значення (`MAX`) -
мінімальне значення (`MIN`)

для стовпчика `price` таблиці `products`.

------------------------------------------------------------------------

### 3. Унікальні значення та сортування

-   Обрати унікальні значення колонок `category_id` та `price` з таблиці
    `products`
-   Відсортувати результат за спаданням `price`
-   Обмежити вивід до 10 рядків

Використовуються `DISTINCT`, `ORDER BY`, `DESC`, `LIMIT`.

------------------------------------------------------------------------

### 4. Фільтрація та підрахунок рядків

Знайти кількість продуктів, ціна яких знаходиться в діапазоні від **20
до 100** включно.

Використовуються `COUNT(*)` та `BETWEEN`.

------------------------------------------------------------------------

### 5. Групування даних

Для кожного постачальника (`supplier_id`) знайти: - кількість
продуктів - середню ціну продукту

Використовуються `GROUP BY`, `COUNT`, `AVG`.

------------------------------------------------------------------------

## ▶️ Як запустити

1.  Відкрити **MySQL Workbench**
2.  Підключитися до MySQL-сервера
3.  Обрати потрібну схему бази даних
4.  Відкрити файли `sql*.sql`
5.  Виконати SQL-запити по черзі

------------------------------------------------------------------------

## 📎 Примітки

-   Структура таблиць відповідає навчальній базі даних
-   Усі рішення написані з урахуванням синтаксису MySQL

------------------------------------------------------------------------
